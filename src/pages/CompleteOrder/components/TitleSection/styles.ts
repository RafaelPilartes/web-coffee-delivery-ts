import styled from 'styled-components'

export const TitleSectionConteiner = styled.div`
  display: flex;
  gap: 0.5rem;

  > div {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
`
